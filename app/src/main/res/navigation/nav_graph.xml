<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authAsk">

    <fragment
        android:id="@+id/authAsk"
        android:name="com.example.spentanalyzer.AuthAsk"
        android:label="fragment_auth_ask"
        tools:layout="@layout/fragment_auth_ask" >
        <action
            android:id="@+id/action_authAsk_to_loginActivity"
            app:destination="@id/loginActivity" />
    </fragment>
    <activity
        android:id="@+id/loginActivity"
        android:name="com.example.spentanalyzer.ui.login.LoginActivity"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />

</navigation>